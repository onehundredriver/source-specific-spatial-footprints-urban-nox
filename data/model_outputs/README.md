# Model outputs

This folder is reserved for outputs generated by the experimental reproduction scripts.

Recommended output structure:

```text
spatial_1hour/
temporal_1hour/
temporal_24hour/
temporal_168hour/
temporal_31day/
```

Recommended files in each experiment output folder:

```text
run_config_used.json
run_metadata.json
feature_list_used.csv
best_params.json
fold_assignments_used.csv
fold_metrics.csv
cv_summary_metrics.csv
shap_values_val.parquet
val_data_for_shap.parquet
average_builtin_importance.csv
average_permutation_importance.csv
```

Large model-output files should be included only when they are required for reproducing figures or auditing the reported results.
